/*import Vue from 'vue'
import Vuex from 'vuex'
import createPersistedState from "vuex-persistedstate";


Vue.use(Vuex)

export default new Vuex.Store({
  state: {
    count: 0
  },
  mutations: {
  },
  actions: {
  },
  modules: {
  },
  // Wenn eine Variable über die Seitenaufrufdauer hinaus gespeichert werden soll, hier auf das StoreModul verweiesen (und die clear-Bedingungen definieren)
  plugins: [createPersistedState({
        paths: [],
        key: 'vuex',
        reducer: function (state) {
            //clear the state that is persisted in the local storage when the user is logged out
        },
    })],
});*/
